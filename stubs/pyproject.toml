[project]
name = "lzf-stubs"
version = "0.0.1"
description = "Local type stubs for python-neo-lzf's lzf module (dev-only, not published)"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
force-include = { "lzf-stubs" = "lzf-stubs" }
